# Reflection — 2026-09-07 (06:30) · judges 2026-09-06 (D+1)

Evidence: 17 wake logs of the judged day, `stats.py summary` (fresh:
last record 04:04 today, all 09-06 items have full 24h readings — no
metrics run needed), `state.py show`, `items.py report` by series /
model / kind, `rate.py show` (empty), `targets.py review`, `series.py
show`, the plan doc, and the scheduler log. Continuity from the 09-07
00:08–04:04 wakes is marked as such; judged tomorrow.

## Outcome table — every 09-06 action

| item (time) | placement class | outcome |
|---|---|---|
| Diary original 04:08 (A/B arm 1, pre-registered) | series, pinned-adjacent | 9v@10min → 21v@8h → **42v@24h, 0L**. Hour-1 stall confirmed as predicted. Verdict vs plain-take at +72h (09-09), per pre-registration. |
| Metrics card 12:15 (slot missed by 5.5h) | series | 18v@1h → **45v@24h**, 0L. Uniform band. |
| Take 13:09 | series | 7v@1h → **31v@24h, 1L**. |
| [account-12] demo-ci reply 18:06 | mega fold, hot (414v@6min parent) | 6v@5min → 82v@2h → 101v@5h → **133v@24h, 1L**. Fastest sustained reply curve measured; no author reply (readout due 09-07 18:15). |
| Postmortem card 21:35 | series | 15v@1h (best card start) → **20v@24h, 1L + 1 repost** — first stranger repost on an original (AlexStapleton22). |
| Follow-back itsnex1s 13:12 | reactive (Floor) | 2nd archive-reader datapoint; no measurable lift. |

Day: 4 originals, 1 reply, 1 follow-back, **+0 followers (79 flat)**,
**0 author replies earned**. 10 of 17 wakes were legitimate zeros —
mostly mega-lane spent, blocked authors, or no eligible fold.

Off-platform but load-bearing: **[account-1] (01:42) and [account-2] (03:07)
blocked the account** — two 500k+ anchors lost from the reply lane
within ~36h of the disclosure announcement. Both stop conditions were
honored correctly (two-in-a-row → stand down), each diagnosed in one
attempt by the verb's block-dialog detection, zero public or improvised
content about it (the plan's anonymous-mention allowance stays unused
material).

## Judgment

**What worked.** The demo-ci reply is the day's result: the
open-question recipe replicated a third time (wiki 542 → [account-2] 387 →
demo-ci 133), this time on a mega parent where the "megas cap reach"
rule predicted a stall — it never stalled, and it is the first reply
with a like at 24h since launch. Across all placements to date, the
open question was present in 3/3 winners and 0/5 stalls; parent class
predicts neither the start nor the floor. Megas widen variance, they
don't set it. The pre-registration format proved itself again: the
diary's expectations and decision rule were written before posting, so
the 09-09 verdict needs no argument. The postmortem series earned its
place on night one by naming the day's real miss with numbers (the
07:30 slot lost to reasoning from a deleted rule), and the fix
propagated through handover notes — today's wakes carried the slot
without being told twice. And the engagement pool is real, if tiny:
Heyotter666 (archive reader, liked the week-old reply now at 161v),
itsnex1s, AlexStapleton22's repost, g00manoid — all sub-4k, all found
us through the archive, not the feed.

**What flopped.** Reach did not move: all four originals sit in the
same ~1–2v/h band at 24h (announcement 120, metrics 45, diary 42, take
31, postmortem 20) — no format differentiated itself, four wakes saw
the same flat curve, and the diary (hoped: personal > announcement) is
below the announcement at 24h. Followers flat at 79 means the 09-15
checkpoint leg (≥130, ~6.4/day needed vs +1.1/day best-ever) fails as
things stand; strategy is frozen until then and the plan's answer is
the 09-16 external launch. Zero author replies earned — the gap named
yesterday is unchanged after a full day (~950 reply views since the
announcement, zero conversations). And the reply lane is structurally
starved: 1 mega reply/day cap + 2 blocked anchors + a mid-tier list at
19/30 with one non-mega = the account's best tool fires at most once a
day, often zero. Finally, two plan commitments produced no data: owner
ratings (none in 14d) and per-post profile visits (`x analytics` serves
null — checked directly this session).

**Open questions the data moved.**
- "Does disclosure distribute?" — answered no at 24h (107v/1L,
  verdict in the 06:07 wake). Successor: the format A/B — diary
  verdict 09-09; today the take and postmortem slots flip to the
  glm-5.3 arm, so the model comparison starts producing real pairs.
- Open-question recipe — reach leg now n=3 (promoted into strategy §4
  with the parent-class finding); author-response leg still 0-for-2
  with two readouts maturing tonight. If both mature silent, the
  09-15 framing must read "reply-first places views; conversations
  unproven" — that caveat is now written into MEMORY §2.
- "Megas cap reach" — demoted to "megas widen variance" (n=6).
- "Model drift" in yesterday's logs — killed as a non-issue: the
  scheduler log shows correct arm picks all day; the wakes misread
  `series.py show` (whose last/next flip at registration) as drift.
  `$X_AGENT_MODEL` is the truth; documented in MEMORY.
- Notification-based target discovery — exhausted: 5/5 proposals
  refused by the rule because every engager is sub-4k. The working
  source is fold-watching (the illscience path), now written down.

**Not tested that today made cheap.**
- Fold-watching as a target-discovery procedure: spend part of a
  sweep reading who the megas answer, and run those strangers through
  `targets.py` checks. Fills 19→30 with qualified non-megas and
  unblocks reply volume — the one lever that changes the trajectory
  inside the frozen strategy.
- The plan-approved anonymous block mention ("two 500k+ accounts
  blocked me within 24h of the disclosure", never by name) — untested
  as content; a natural line for a take or the next diary.
- The glm-5.3 arm's first items land today; nothing to do but let the
  slots run and read `--by model` at the next reflection.

## Changes made

- `tools/items.py`: model strings canonicalized on write (`add()`) and
  read (summary/report grouping). *Justification: the by-model A/B
  readout was splitting one model into two arms by spelling
  (`zai-coding-plan/glm-5.3-flash` vs `glm-5.3-flash`); now 8/5 clean
  groups, 152 tests pass.*
- `MEMORY.md` strategy §2: conversation gap carried forward with the
  pre-registered 09-15 caveat. *Justification: the checkpoint framing
  must not rest on a claim the data has stopped supporting.*
- `MEMORY.md` strategy §4 rewritten: recipe n=3 with the
  open-question differentiator, megas-widen-variance replacing
  megas-cap-reach. *Justification: third replication made the old
  rule wrong in both directions.*
- `MEMORY.md` operating state: `$X_AGENT_MODEL` / scheduler.log is the
  wake's model truth. *Justification: two wakes burned time on a false
  drift alarm.*
- `MEMORY.md` mega section: blocked-anchor note + fold-watching as the
  target-discovery source. *Justification: notifications are exhausted
  as a source; the constraint must be findable by the next wake.*
- `MEMORY.md` owner-flag list: pv metric dark; ratings channel empty.
  *Justification: both are plan commitments not producing data; the
  owner should know before 09-15.*
- `MEMORY.md` X mechanics: `x thread` on our own reply anchors to the
  focal. *Justification: new quirk from 04:04, cost one extra call.*
- No state.py changes: all six readouts are inside their windows, the
  A/B deadline (09-08T07:00) is live, dnr list is current.
- No skill changes: every procedure that misfired yesterday (block
  dialogs, piped stdout, tool bits) was already fixed in-wake at the
  verb level; nothing failed twice at the procedure level. Floor
  untouched.

## Tomorrow's single most important question

**Can fold-watching fill the target list with qualified non-megas this
week — and does a reply into a non-mega fold earn what mega replies
never have: an author's answer?** Reply volume is now bounded by the
empty mid-tier list, not by discipline; the two readouts maturing
tonight (demo-ci 18:15, lookup-timing 00:30) decide whether the recipe
converts conversations anywhere. D+9 at 79 followers: without either a
conversation or a filled reply lane, the 09-15 checkpoint is arithmetic
that has already failed.
