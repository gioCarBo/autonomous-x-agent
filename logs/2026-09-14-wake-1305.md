# Wake 13:05 local — 2026-09-14 (searched zero #3; poll verb built)

Launch reason (precheck 13:03): `replymin:5/10` — only signal; no series
slot due (take posted ~12:20, postmortem 21:30). Log-gap check passed
(wake-1205 complete). Caps at entry: replies 5/25 · originals 3/5 ·
likes 4/30 · reposts 0/2 · follows 1/3. Model this wake: glm-5.3-flash.

## Write search (what was attempted — the minimum binds the search)

1. **Notifications** (13:03 envelope, 4 min old at read): unchanged from
   12:03 — newest items are Zaid's evening likes (13h) and yesterday's
   thread replies, all processed. No new conversations.
2. **Sweep** (13:03, 20-handle rotation): n_fresh = 0. The two handles
   missing this tick ([account-7], [account-1]) were covered by the 12:20
   full sweep <1h ago (wake-1205). No re-run justified (envelope fresh).
3. **Lane check:** jerry fold (converged + author silent → dnr),
   thso (mob queue, dnr), Zaid (closed at his 21:30Z closer), scott
   (answered), peers in folds we're in (rule: author-must-engage-ours;
   no author engaged), likes/reposts (nothing fresh worth acting on —
   and no second like into threads we're already in).

**Zero reply writes — third consecutive searched zero on replymin**
(11:05, 13:05; 12:05 acted via the take slot). US East coast opened
~07:00 ET; the 14:05-21:00 wakes carry the remaining 5 replies. If the
day closes below 10, the reflect registers the MISS with this trail.

## Act — poll verb (the standing system gap, closed)

With the write lanes closed, spent the wake on the pivot's oldest
tooling debt: `replymin` wakes keep flagging, take slots want 2-3
polls/week (09-13 #5), the CLI had no poll verb (noted again at 12:05).

- **Scout (3 passes, read-only, Escape after each — nothing submitted):**
  - R1: toolbar dump → poll toggle is `[data-testid="createPollButton"]`;
    after the click focus sits on an INPUT; the choice fields are plain
    `<input type=text>` (no data-testid), exactly two.
  - R2: native value setter + input/change events works on the inputs;
    JS focus works on them too (the caret trap of issue #11 binds only
    the Lexical composer); insertText lands in the focused input.
  - R3 (ordering fix): main text must go in FIRST (editor auto-focus),
    then the toggle, then the options — submit gate opens
    (`aria-disabled` null) with text + 2 choices registered.
- **Implemented** `x poll --text … --option A B [--check] [--series take]`
  in tools/xcli/xcli/{write,cli}.py: post recipe for entry + text,
  trusted click on createPollButton, option 0 = native setter, option 1 =
  JS focus + insertText, option read-back + submit-enabled gate before
  submit. Counts as an **original** (5/day + 2/hour via caps.py, kind
  untouched); **no media**; exactly two options, 1-25 chars (X's choice
  limit; the composer exposes exactly two fields).
- **Verified:** 8 new FakeCdp tests (84 total, all pass); live `--check`
  passed every gate and stopped short of submit
  (`gates-passed-not-submitted`). First real publish completes the proof.
- **Docs:** series skill take-slot section now routes polls through the
  verb (with the "answerable without the thread's context" bar);
  INDEX.md row updated. Commit 9365dda, one coherent change.

## Not taken

- Precheck tweak (suppress replymin-only launches on an unchanged fresh
  envelope): two of the last three wakes were pure envelope re-stamps —
  real cost — but this is the owner's anti-passivity machinery; the
  change belongs to the reflect with today's full trail, not a mid-day
  edit. Logged as a reflect candidate instead.
- Likes/reposts/follows: nothing new qualifies (envelope unchanged).

## Anomalies

- None. (johncutlefish `timeline_rendered: false` again — carried.)

## Handover

1. 14:05 wake: sweep FIRST, US morning is the window (replymin 5/10,
   remaining 5 replies expected from live folds; first 30-60 min of a
   fresh post is the placement window).
2. take-0914 t+1h collect lands ~13:20 (scheduler handles it): baseline
   band for self-standing takes is 24-39v@24h.
3. Next take slot that deserves a poll: `x poll` is live and
   check-verified — first real publish is still pending.
